% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmfkcreg.R
\name{nmfkcreg.cv}
\alias{nmfkcreg.cv}
\title{Performing k-fold cross validation on NMF (Non-negative Matrix Factorization) with kernel covariates regression}
\usage{
nmfkcreg.cv(
  Y,
  A = diag(ncol(Y)),
  Q = 2,
  gamma = 0,
  epsilon = 1e-04,
  maxit = 5000,
  div = 5,
  seed = 123,
  method = "EU"
)
}
\arguments{
\item{Y}{observation matrix}

\item{A}{kernel matrix. Without covariates, identity matrix is used.  Or matrix A(R,N) having N columns can be accepted.}

\item{Q}{rank of basis matrix and Q<=min{P,N}}

\item{gamma}{penalty parameter for C(Q,N) in objective function}

\item{epsilon}{positive convergence tolerance}

\item{maxit}{maximum number of iterations}

\item{div}{number of partition usually described as "k" of k-fold}

\item{seed}{integer used as argument in set.seed function
which controls the reproducibility of the partition.}

\item{method}{default objective function is Euclid distance "EU", otherwise Kullback–Leibler divergence "KL"}
}
\value{
objfunc: last objective function

objfunc.block: objective function at each block

block: partition block index {1,...,div} assigned to each column of Y
}
\description{
\code{nmfkcreg.cv} apply cross validation method for k-partitioned columns of Y(P,N)
}
\examples{
library(nmfkcreg)
Y <- t(iris[,-5])
result <- nmfkcreg.cv(Y,Q=2)
table(result$block)
result$objfunc
}
