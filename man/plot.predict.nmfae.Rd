% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmfae.R
\name{plot.predict.nmfae}
\alias{plot.predict.nmfae}
\title{Plot method for predict.nmfae objects}
\usage{
\method{plot}{predict.nmfae}(x, ...)
}
\arguments{
\item{x}{An object of class \code{"predict.nmfae"} returned by \code{\link{predict.nmfae}}.}

\item{...}{Additional graphical parameters passed to \code{plot} or \code{image}.}
}
\value{
Invisible \code{NULL}. Called for its side effect (plot).
}
\description{
For \code{type = "response"}: if actual values \eqn{Y_1} were stored,
displays an observed-vs-predicted scatter plot with \eqn{R^2} in the title.
Otherwise, displays the predicted matrix as a heatmap.

For \code{type = "class"}: if actual classes were stored, displays a
confusion matrix heatmap with accuracy (ACC) in the title.
}
\examples{
\donttest{
set.seed(1)
Y <- matrix(runif(20), nrow = 4)
res <- nmfae(Y, rank1 = 2)
pred <- predict(res)
plot(pred)
}
}
\seealso{
\code{\link{predict.nmfae}}
}
\keyword{internal}
