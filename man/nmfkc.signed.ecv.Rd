% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmfkc.signed.R
\name{nmfkc.signed.ecv}
\alias{nmfkc.signed.ecv}
\title{Element-wise cross-validation for nmfkc.signed}
\usage{
nmfkc.signed.ecv(Y, A, rank = 1:3, ...)
}
\arguments{
\item{Y}{Real-valued \eqn{Q_{\mathrm{obs}} \times N} response matrix
(signed entries allowed).}

\item{A}{Real-valued \eqn{D \times N} covariate matrix (signed).}

\item{rank}{Integer vector of candidate ranks (default \code{1:3}).}

\item{...}{Passed to \code{\link{nmfkc.signed}}; also accepts
\code{nfolds} (default 5; \code{div} alias), \code{seed}
(default 123).}
}
\value{
A list with \code{objfunc} (MSE per rank), \code{sigma}
(RMSE), \code{objfunc.fold} (per-fold per-rank), \code{folds},
\code{Q.grid}.
}
\description{
Element-wise k-fold CV (Wold's CV): held-out elements
are masked via \code{Y.weights = 0} during fitting, and the RMSE on
those elements is reported.  Loops over candidate \code{rank} values.
}
\section{Lifecycle}{

This function is \strong{experimental}.
}

\seealso{
\code{\link{nmfkc.signed}}, \code{\link{nmfkc.signed.cv}}
}
