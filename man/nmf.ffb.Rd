% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmf.ffb.R
\name{nmf.ffb}
\alias{nmf.ffb}
\alias{nmf.ffb.inference}
\alias{nmf.ffb.cv}
\alias{nmf.ffb.DOT}
\alias{nmf.ffb.split}
\title{NMF-FFB: NMF with Feed-Forward and Feedback structure}
\usage{
nmf.ffb(
  Y1,
  Y2,
  rank = NULL,
  X.init = "nndsvd",
  X.L2.ortho = 100,
  C1.L1 = 1,
  C2.L1 = 0.1,
  epsilon = 1e-06,
  maxit = 5000,
  seed = 123,
  ...
)

nmf.ffb.inference(
  object,
  Y1,
  Y2,
  B = 1000L,
  threshold = 0.01,
  ci.level = 0.95,
  C1.L1 = 1,
  C2.L1 = 0.1,
  seed = 123L,
  ...
)

nmf.ffb.cv(...)

nmf.ffb.DOT(result, ...)

nmf.ffb.split(x, ...)
}
\arguments{
\item{Y1, Y2}{Non-negative data matrices (endogenous and exogenous blocks).}

\item{rank}{Integer basis rank (\code{NULL} lets the model choose).}

\item{X.init}{Basis initialization method (default \code{"nndsvd"}); accepts
the same menu as \code{\link{nmfkc}} (e.g. \code{"kmeans"}, \code{"kmeans++"}).}

\item{X.L2.ortho, C1.L1, C2.L1}{Non-negative penalty parameters (basis
orthogonality, and L1 on the two coefficient blocks).}

\item{epsilon, maxit}{Convergence tolerance and maximum iterations.}

\item{seed}{Integer RNG seed.}

\item{...}{Further arguments passed to the underlying implementation.}

\item{object}{A fitted \code{nmf.ffb} object (for \code{nmf.ffb.inference}).}

\item{B}{Number of bootstrap replicates.}

\item{threshold}{Minimum coefficient magnitude to display / test.}

\item{ci.level}{Confidence level for bootstrap intervals.}

\item{result}{A fitted object (for \code{nmf.ffb.DOT}).}

\item{x}{A fitted object (for \code{nmf.ffb.split}).}
}
\value{
As the corresponding fitter / helper (fits carry the \code{"nmf.ffb"}
class in addition to \code{"nmf.sem"}).
}
\description{
\code{nmf.ffb} and the \code{nmf.ffb.*} functions fit NMF-FFB
(Non-negative Matrix Factorization with Feed-Forward + Feedback), the
canonical name adopted in Satoh (2025), arXiv:2512.18250. They are thin
wrappers over the internal \code{nmf.sem*} implementation (an earlier name
for the same model); result objects carry \code{class = c("nmf.ffb",
"nmf.sem")} so all S3 methods are reused by inheritance.
\itemize{
\item \code{nmf.ffb()} fits the model.
\item \code{nmf.ffb.inference()} adds bootstrap inference on the feedback /
feed-forward coefficients.
\item \code{nmf.ffb.cv()} performs cross-validation for rank selection.
\item \code{nmf.ffb.DOT()} renders the fitted structure as a Graphviz graph.
\item \code{nmf.ffb.split()} heuristically splits variables into
exogenous / endogenous groups.
}
}
\references{
Satoh, K. (2025). arXiv:2512.18250.
}
\seealso{
\code{\link{nmfkc}}, \code{\link{nmfkc.DOT}}
}
